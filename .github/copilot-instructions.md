# Initial project structure for movie data analysis

# Backend
- Created a folder named `backend` with a basic Express server in `server.js`.

# Frontend
- Created a folder named `frontend` with a basic React app structure, including `index.js` and `App.js`. 
